import { ResourceOptions } from '../../help/type/IResource';

const resource: ResourceOptions = {
	name: '任务执行',
	value: 'task',
	description: '触发、停止与查询常规任务执行记录',
	order: 60,
};

export default resource;
